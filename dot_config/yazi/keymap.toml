# A TOML linter such as https://taplo.tamasfe.dev/ can use this schema to validate your config.
# If you encounter any issues, please make an issue at https://github.com/yazi-rs/schemas.
#:schema https://yazi-rs.github.io/schemas/keymap.json

[mgr]
# Default keymaps are at: https://github.com/sxyazi/yazi/blob/shipped/yazi-config/preset/keymap-default.toml
# I include the relevant defaults that are being overriden as comments
# so I can remember what they would otherwise be
prepend_keymap = [
    # Find
    # { on = "/", run = "find --smart",            desc = "Find next file" },
    # { on = "?", run = "find --previous --smart", desc = "Find previous file" },
    { on = "\\", run = "find --previous --smart", desc = "Find previous file" },

    # Help
    # { on = "~",    run = "help", desc = "Open help" },
    # { on = "<F1>", run = "help", desc = "Open help" },
    { on = "?",    run = "help", desc = "Open help" },

]
